name: Frontend CI

on:
  pull_request:
    branches: [master]
    paths:
      - "kanban-frontend/**"
      - ".github/workflows/frontend-ci.yml"
  push:
    branches: [master]
    paths:
      - "kanban-frontend/**"
      - ".github/workflows/frontend-ci.yml"

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: kanban-frontend

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: kanban-frontend/package-lock.json

      - run: npm ci
      - run: npm run lint
      - run: npm run build
