server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # react-router-dom(BrowserRouter) 새로고침/직접 URL 접속 시 404 방지
    location / {
        try_files $uri $uri/ /index.html;
    }
}
